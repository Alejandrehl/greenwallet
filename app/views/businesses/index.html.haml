%h1.text-center Mis negocios

%table.table.table-striped{:id => "businessTable"}
  %thead
    %tr
      %th Name
      %th Description
      %th User
      %th
      %th
      %th

  %tbody
    - @businesses.each do |business|
      %tr
        %td= business.name
        %td= business.description
        %td= business.user.email
        %td= link_to (fa_icon "eye") ,business, class: "btn btn-primary btn-block"
        %td= link_to (fa_icon "edit"), edit_business_path(business), class: "btn btn-primary btn-block"
        %td= link_to (fa_icon "trash"), business, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary btn-block"

%br

= link_to (fa_icon "plus-circle"), new_business_path, class: "btn btn-primary float-right"